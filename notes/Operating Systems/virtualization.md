	 
- **virtualization** (abbr. **v12n**)
- **virtual machine** (**VM**)
- **emulator**
- **system virtual machine** (also called **SysVM**), full virtualization
- **hypervisor** (also known as a **virtual machine monitor** (**VMM**) or **virtualizer**)
   ![300](https://upload.wikimedia.org/wikipedia/commons/9/9e/Hyperviseur.svg)
	- Type-1 (native or bare-metal hypervisors)
	- Type-2 (or hosted hypervisors)
- **hardware virtualization**
	- the _host_ machine is the machine that is used by the virtualization
	- the _guest_ machine is the virtual machine
	- types:
		- **full virtualization**
		- **paravirtualization**
		- **hybrid virtualization**
- **process virtual machine** (sometimes called an _application virtual machine_, or _Managed Runtime Environment_ (MRE))
- **OS-level virtualization**
- **containerization**
	- OS containers
	- Application containers



